import template from './blr-two-col-flex-preview.html.twig';

const { defineComponent } = Shopware.Vue;

Shopware.Component.register('cms-block-blr-two-col-flex-preview', defineComponent({
    template,
}));
